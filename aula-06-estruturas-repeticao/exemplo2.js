// Importando o módulo
const prompt = require('prompt-sync')();

// Declarando variável de controle
let contador;

// Estrutura de repetição para com decremento
for (contador = 10; contador >= 1; contador--) {

    // Exibe os números na tela
    console.log(contador);
}

// Mensagem final
console.log('Fogo!');